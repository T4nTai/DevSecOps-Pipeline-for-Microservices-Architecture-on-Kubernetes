apiVersion: v1
kind: ConfigMap
metadata:
  name: burst-autoscaler-state
  namespace: kube-system
data:
  last_active: "0"    # unix timestamp lần cuối có pending pods
  status: "idle"      # idle | scaling-up | running | scaling-down
