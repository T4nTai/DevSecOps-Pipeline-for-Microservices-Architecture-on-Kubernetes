# Ignore everything at root, then allowlist what should be committed
/*

# Core project files
!README.md
!connect.sh
!secrets.list
!docker-compose.yml

# Source directories
!scripts/
!k8s/
!terraform/
!jenkins/
!app/
app/*
!app/src/
!app/src/**

# Terraform — never commit state or local overrides
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*_override.tf
*_override.tf.json
secrets.tfvars
*.tfvars.json

# Terraform — runtime-generated and environment-specific (contain real IPs, AWS account IDs)
terraform/inventory.ini
terraform/**/terraform.tfvars
terraform/**/*-tools.tfvars
terraform/**/*-apps.tfvars

# Kubespray
kubespray/.venv/
kubespray/kubespray-src/

# Runtime state — never commit
.deploy-state.env
.env
.env.secret
.kubeconfig

# App build artifacts
app/src/**/.gradle/
app/src/**/bin/
app/src/**/build/
app/src/**/node_modules/
app/src/**/__pycache__/
app/src/**/*.class
